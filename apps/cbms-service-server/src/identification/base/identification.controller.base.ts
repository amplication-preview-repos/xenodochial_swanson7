/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { IdentificationService } from "../identification.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { IdentificationCreateInput } from "./IdentificationCreateInput";
import { Identification } from "./Identification";
import { IdentificationFindManyArgs } from "./IdentificationFindManyArgs";
import { IdentificationWhereUniqueInput } from "./IdentificationWhereUniqueInput";
import { IdentificationUpdateInput } from "./IdentificationUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class IdentificationControllerBase {
  constructor(
    protected readonly service: IdentificationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Identification })
  @nestAccessControl.UseRoles({
    resource: "Identification",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createIdentification(
    @common.Body() data: IdentificationCreateInput
  ): Promise<Identification> {
    return await this.service.createIdentification({
      data: data,
      select: {
        address: true,
        contactNumber: true,
        createdAt: true,
        dateOfBirth: true,
        email: true,
        fullName: true,
        gender: true,
        id: true,
        maritalStatus: true,
        nationalId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Identification] })
  @ApiNestedQuery(IdentificationFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Identification",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async identifications(
    @common.Req() request: Request
  ): Promise<Identification[]> {
    const args = plainToClass(IdentificationFindManyArgs, request.query);
    return this.service.identifications({
      ...args,
      select: {
        address: true,
        contactNumber: true,
        createdAt: true,
        dateOfBirth: true,
        email: true,
        fullName: true,
        gender: true,
        id: true,
        maritalStatus: true,
        nationalId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Identification })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Identification",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async identification(
    @common.Param() params: IdentificationWhereUniqueInput
  ): Promise<Identification | null> {
    const result = await this.service.identification({
      where: params,
      select: {
        address: true,
        contactNumber: true,
        createdAt: true,
        dateOfBirth: true,
        email: true,
        fullName: true,
        gender: true,
        id: true,
        maritalStatus: true,
        nationalId: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Identification })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Identification",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateIdentification(
    @common.Param() params: IdentificationWhereUniqueInput,
    @common.Body() data: IdentificationUpdateInput
  ): Promise<Identification | null> {
    try {
      return await this.service.updateIdentification({
        where: params,
        data: data,
        select: {
          address: true,
          contactNumber: true,
          createdAt: true,
          dateOfBirth: true,
          email: true,
          fullName: true,
          gender: true,
          id: true,
          maritalStatus: true,
          nationalId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Identification })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Identification",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteIdentification(
    @common.Param() params: IdentificationWhereUniqueInput
  ): Promise<Identification | null> {
    try {
      return await this.service.deleteIdentification({
        where: params,
        select: {
          address: true,
          contactNumber: true,
          createdAt: true,
          dateOfBirth: true,
          email: true,
          fullName: true,
          gender: true,
          id: true,
          maritalStatus: true,
          nationalId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
